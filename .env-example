# filepath: c:\Users\whlsl\IdeaProjects\Cal-Todo-CRUD\.env
# NOTE: This file previously contained a sensitive OPENAI_API_KEY which has been removed.
# Keep your real keys locally and do NOT commit them to git. Use .env (ignored) or system env vars.

SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# PostgreSQL 데이터베이스 설정
DB_URL=jdbc:postgresql://localhost:5432/caltodo
DB_USERNAME=root
DB_PASSWORD=root123

# AI 설정 (Claude)
APP_AI_ENABLED=true
# OpenAI API 키
OPENAI_API_KEY=your_openai_api_key_here